{% set name = "records" %}
{% set version = "0.5.1" %}
{% set compress_type = "tar.gz" %}
{% set hash_type = "sha256" %}
{% set hash = "f3946f19d5c7cdfe02acff2cb461afb609119171f3990fef5335d72a8992c788" %}
{% set build = 0 %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.{{ compress_type }}
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.{{ compress_type }}
  {{ hash_type }}: {{ hash }}

build:
  noarch: python
  entry_points:
    - records = records:cli
  number: {{ build }}
  script: python setup.py install --single-version-externally-managed --record=record.txt

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - sqlalchemy
    - tablib
    - pandas
    - docopt

test:
  imports:
    - records

  commands:
    - records --help

about:
  home: https://github.com/kennethreitz/records
  license_file: LICENSE
  license: ISC
  license_family: OTHER
  summary: 'SQL for Humans'
  dev_url: https://github.com/kennethreitz/records

extra:
  recipe-maintainers:
    - pmlandwehr
